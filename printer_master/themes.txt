@name Themes
@persist Themes:table SelectedTheme:string

SelectedTheme = "Dark"
Themes = table()

function vector getThemeVec(Element:string) {
    return Themes[SelectedTheme, table][Element, vector]
}

function string getThemeString(Element:string) {
    return Themes[SelectedTheme, table][Element, string]
}

function number getThemeNumber(Element:string) {
    return Themes[SelectedTheme, table][Element, number]
}

function number setTheme(Theme:string) {
    if (Themes:exists(Theme)) {
        SelectedTheme = Theme
        return 1
    } else { 
        return 0
    }
}

Themes["Dark", table] = table()
Themes["Dark", table]["Background", vector] = vec(0,0,0)    #Background Color
Themes["Dark", table]["FontSize", number] = 30              #Font Size
Themes["Dark", table]["FontColor", vector] = vec(0,0,0)     #Font Color
Themes["Dark", table]["Foreground", vector] = vec(0,0,0)    #Foreground Color

Themes["Light", table] = table()
Themes["Light", table]["Background", vector] = vec(0,0,0)    #Background Color
Themes["Light", table]["FontSize", number] = 30              #Font Size
Themes["Light", table]["FontColor", vector] = vec(0,0,0)     #Font Color
Themes["Light", table]["Foreground", vector] = vec(0,0,0)    #Foreground Color

Themes["White", table] = table()
Themes["White", table]["Background", vector] = vec(0,0,0)    #Background Color
Themes["White", table]["FontSize", number] = 30              #Font Size
Themes["White", table]["FontColor", vector] = vec(0,0,0)     #Font Color
Themes["White", table]["Foreground", vector] = vec(0,0,0)    #Foreground Color
